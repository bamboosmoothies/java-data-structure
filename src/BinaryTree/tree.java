package BinaryTree;

public class tree {
    /**
     * tree = a non-linear data structure where nodes are organized in a hierarchy
     * EX. family tree
     * used in file explorer, databases, dns, HTML DOM
     * 
     * root nodes - top of the tree; no incoming edges, only outgoing edges ;
     * parents
     * leaf nodes - no outgoing edges, only incoming edges ; child
     * branch nodes - incoming & outgoing edges ; parents & child
     * siblings = share nodes
     * subtree = a smaller tree held within a larger tree
     * size of tree = # of nodes
     * depth = # of edges below the root node
     * height = # of edges above furthest leaf node (height of 0 starts at bottom,
     * ++)
     * 
     * 
     */
}
